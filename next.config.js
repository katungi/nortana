/** @type {import('next').NextConfig} */
// const middleware = require('./src/middleware.ts');

const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true
  },
}


module.exports = nextConfig
